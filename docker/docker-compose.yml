version: '3'

networks:
  nuxt-network:
    driver: bridge
  default:


volumes:
  public-storage:
  node_modules: {}

services:
  rentcaruae_frontend:
    build:
      dockerfile: ../docker/nuxt/Dockerfile
      context: './../frontend'
      args:
        - BASE_URL=https://147.45.139.233:5002
        - API_URL=https://147.45.139.233:5003/api
        - BACK_URL=https://147.45.139.233::5003
    image: nginx:latest
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    restart: "no"
    ports:
      - "24681:24678"
      - "5002:3000"
    networks:
      - nuxt-network
      - default
